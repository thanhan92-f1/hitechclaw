{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Node (xClaw server in Docker)",
            "type": "node",
            "request": "attach",
            "address": "localhost",
            "port": 9229,
            "localRoot": "${workspaceFolder}/xClaw-main",
            "remoteRoot": "/app",
            "skipFiles": [
                "<node_internals>/**"
            ]
        }
    ]
}
